export interface Post {
    title: string;
    views: number;
  }
  
  export interface Lesson {
    title: string;
    duration: string;
  }
  
  export interface GuestbookEntry {
    name: string;
    message: string;
    date: string;
  }